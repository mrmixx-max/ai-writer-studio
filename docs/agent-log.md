# Agent Log

## Sprint 7 — Agent 2: Ollama-Performance (Connection-Pool, Prompt-Cache, kompakte Prompts, Benchmark)

[AGENT-2] [src/services/ollama/connectionPool.ts + connectionPool.test.ts] NEU (Teilaufgabe 1 — Connection Pooling): OllamaConnectionPool begrenzt parallele Requests pro Ollama-Instanz auf maxConcurrent (Default 4 = OLLAMA_NUM_PARALLEL-Default) via FIFO-Queue statt ungebremster Parallelität. acquire()/release-Vertrag: der Slot gilt für die GESAMTE Stream-Dauer (nicht nur bis zum Response-Header — sonst unterlaufen parallele Streams die Grenze); release ist idempotent (kein Doppel-Freigabe-Leak), run() kapselt den try/finally-Vertrag für Promise-Arbeit. Queue-Timeout (Default 60 s, 0 = unbegrenzt) weist wartende Requests mit sprechendem Fehler ab; PoolStats (active/queued/completed/rejected/totalWaitMs/maxWaitMs/avgWaitMs) für Diagnose. Singleton pro baseUrl via getOllamaPool (pro Server genau eine Ressourcen-Grenze), resetOllamaPools() für Tests. | Interface-Impact: ja (additiv, neues Modul src/services/ollama/) | Tests: 14 (Begrenzung maxConcurrent=2/1, FIFO-Reihenfolge, manuelles acquire/release, finally-Vertrag bei Fehler, Doppel-Release-Idempotenz, Queue-Timeout mit Fake-Timern, rejected-Zähler, timeout=0, Wartezeit-Statistik, Singleton/Reset)

[AGENT-2] [src/services/ollama/promptCache.ts + promptCache.test.ts] NEU (Teilaufgabe 2 — Response-Caching): PromptCache mit TTL (Default 10 min) und LRU-Eviction (Default 200 Einträge) über promptCacheKey = FNV-1a-Hash von Modell + Messages + temperature/maxTokens (timeoutMs bewusst NICHT im Key — ergebnisirrelevant). Verfallene Einträge zählen als Miss mit expired-Zähler; getOrCompute() als Standard-Einstiegspunkt (berechnet nur bei Miss, legt nur Strings ab, Fehler werden nicht gecacht); peek() ohne Statistik-Seiteneffekt, purgeExpired()/clear()/delete() für Housekeeping; getStats() mit hitRate. Singleton getPromptCache()/resetPromptCache(). | Interface-Impact: ja (additiv) | Tests: 18 (set/get, TTL-Verfall mit Fake-Timern inkl. ttl=0-never-expire, LRU-Touch + Verdrängung, replace ohne Eviction, purgeExpired, hitRate, clear, Key-Determinismus + Temperatur-Fluss, getOrCompute hit/miss/kein-Fehler-Caching, Singleton)

[AGENT-2] [src/services/llm/ollama.ts ERWEITERT + src/types/llm.ts + ollama.poolcache.test.ts] VERDRAHTUNG (Akzeptanz: Pool + Cache WERDEN GENUTZT): OllamaProvider.chat() läuft jetzt über den Connection-Pool — Slot wird vor dem fetch belegt und erst nach komplettem NDJSON-Stream-Verbrauch freigegeben (Abort-sicher, release idempotent, ProviderError-Meldungen unverändert). Neu (additiv): ChatOptions.cache?: boolean (opt-in) — bei cache:true prüft der Provider den PromptCache vor dem HTTP-Call; Cache-Treffer yieldt die volle Antwort ohne Inferenz (0 ms), Miss streamt normal und legt das Ergebnis ab. Ohne cache:true byte-identisches Sprint-6-Verhalten (kein Breaking Change: alle bestehenden Aufrufer unangetastet, Cache ist bewusster Opt-in für deterministische Aufgaben wie Outline-Re-Render). ChatOptions um ein optionales Feld erweitert — bestehende Signaturen unverändert. | Interface-Impact: ja (additiv: ChatOptions.cache?, intern Pool/Cache-Nutzung) | Tests: 6 neue (Cache-Hit = 1 fetch statt 2, kein Caching ohne Flag, Miss legt ab, verschiedener Prompt = verschiedener Key, parallele Chats respektieren maxConcurrent=2 mit Peak-Messung)

[AGENT-2] [src/services/ollama/compactPrompts.ts + compactPrompts.test.ts] NEU (Teilaufgabe 3 — Streaming-/Token-Optimierung): Kompakte System-Prompts je Modell-Familie (Semantik = Original, Pflicht-Formulierungen "NUR"/Format-Anforderung erhalten — Sprint-3-Befund: lokale Modelle folgen genau diesen Schlüsselwörtern). deepseek: 64 → 26 Tokens (−60 %), qwen: 38 → 27 (−29 %); Ersparnis fällt bei JEDEM Call an (Prompt-Evaluation vor dem ersten Token, Time-to-first-token sinkt, mehr nutzbarer Kontext). compactProfile()/applyCompactProfile() analog applyLocalModelProfile: nur setzen wenn KEIN System-Prompt existiert — aufruferseitige Prompts (Stilprofile, Bookwriter-Roles, Settings-Prompt) gewinnen immer. compactSavings() misst die Ersparnis (~4 Zeichen/Token, Router-Konvention). Bestehende localModelProfiles.ts/aiwriting-Prompts bleiben UNVERÄNDERT — kompakte Varianten sind opt-in. | Interface-Impact: ja (additiv) | Tests: 10 (Familien-Erkennung, Pflicht-Formulierungen, unbekanntes Modell → Default, Ersparnis > 0 und ≤ 50 % Original-Tokens bei deepseek, compactProfile übernimmt maxTokens/temperature, applyCompactProfile fügt hinzu/überschreibt nicht)

[AGENT-2] [scripts/benchmark-ollama.mjs] NEU (Teilaufgabe 4 — Benchmark vor/nachher): misst (1) Cache-Latenz identischer Prompts Baseline vs. Sprint 7, (2) Pool-Spitzenlast/-Queueing bei 2·parallel "Chunk-Requests", (3) Token-Ersparnis der kompakten Prompts (statisch). Default offline: deterministische Simulation über DIESELBEN Code-Pfade (esbuild-Bundle der TS-Module, Agent-Log-Konvention) — CI-tauglich, 0 echte Calls; --real gegen lokalen Ollama-Server (llama3.2:latest). | Benchmark-Ergebnis REAL (je 4 Runs, llama3.2:latest, Lauf 1/Lauf 2): Baseline 449/346/329/363 ms bzw. 22700 (Modell-Kaltstart)/291/419/317 ms pro Call → Sprint 7 mit Cache 496/0/0/0 ms bzw. 286/0/0/0 ms (nur 1 Inferenz-Call, Hit-Rate 75 %, Gesamt-Ersparnis 66.6 % bzw. 98.8 %; jeder weitere identische Prompt ~0 ms statt ~300–450 ms). Simulation: Ersparnis 80.5 % bei 5 Runs. Pool: Spitze 8 → 2 gleichzeitig im gebremsten Modus (ø Queue-Wait 91 ms — Ressourcenschutz gegen KV-Cache-/Speicherdruck, gewollt). | Akzeptanzkriterien: (1) Connection Pool existiert UND wird genutzt ✓ (OllamaProvider-Verdrahtung + Peak-Test), (2) Prompt-Caching mit TTL funktioniert ✓ (Verfalls-Tests + opt-in Cache-Hit ohne zweiten fetch), (3) Benchmark zeigt Verbesserung ✓ (real 66.6 % / offline 80.5 % Gesamt-Ersparnis; −38 Prompt-Tokens/Call bei deepseek). Budget: 0 Cloud-API-Calls (nur lokale Ollama-Inferenz, 8 kurze Calls für den Real-Benchmark). Keine Breaking Changes (ChatOptions um optionales cache?-Feld erweitert; alle bestehenden Call-Pfade verhalten sich unverändert). | Tests: 48 neue (14 Pool + 18 Cache + 10 kompakte Prompts + 6 Provider-Verdrahtung); eslint clean (alle geänderten Dateien); tsc clean im eigenen Scope (services/ollama, services/llm, types/llm) — vorgefundene tsc-Fehler in uncommitteten Fremd-Dateien (kdpUpload*/prompts/styles.test.ts, paralleler Agent-Scope) nicht berührt.

## Sprint 7 — Agent 1: KDP-API-Integration (Automatischer Upload)

[AGENT-1] [src/services/bookwriter/kdpUpload.ts + kdpUpload.test.ts] NEU (Teil 1 — Upload-Service-Fassade): uploadToKdp(file, metadata, opts) führt den vollständigen Flow aus: (1) Pre-Upload-Check — Validierungsfehler → Status rejected mit gesammelten Fehlergründen, OHNE Transport zu rufen (per Test verifiziert); (2) Status uploading; (3) Transport über injizierbares uploadFn(pkg) → processing, API-Fehler → rejected mit Grund; (4) pollFn(remoteId) → live/rejected. prepareUpload() liefert ok=false + strukturierte Issues bzw. ok=true + KdpUploadPackage (uploadId, file, metadata, isbn, createdAt, UI-Checkliste); buildUploadPackage() deterministisch bei injizierter now/randomId. createKdpUploadService() als Fassade für CLI/Dashboard: ein UploadTracker, upload(), render() (Fortschrittszeile), statusLabel(). DESIGN-ENTScheidung: Amazon bietet keine öffentliche KDP-Upload-REST-API für Self-Publisher — der Transport ist bewusst als injizierbare uploadFn/pollFn abstrahiert (KDP-Web-Upload heute, offizielle API später), alle Tests offline via Fakes (0 echte API-Calls). | Interface-Impact: ja (additiv: neue Module in src/services/bookwriter/, keine Änderung an Bestands-Signaturen) | Tests: 11 (Status-Flow, Validierungs-Abbruch ohne Transport, API-Fehler → rejected, prepare-only-Modus, Fassade)

[AGENT-1] [src/services/bookwriter/kdpCredentials.ts + kdpCredentials.test.ts] NEU (Teil 2 — Sicheres Credential-Management): createKdpCredentialStore({storage, masterPassphrase}) speichert KDP-OAuth2-Credentials (clientId/clientSecret/refreshToken/marketplace) AUSSCHLIESSLICH verschlüsselt: AES-256-GCM über die bestehende WebCrypto-Infrastruktur (AWS1-Format aus src/services/security/crypto.ts, PBKDF2-SHA256 310k Iterationen) — Rohwert im KV-Store ist Chiffretext, per Test verifiziert (kein Klartext-Substring). Master-Passphrase wird NIE persistiert (nur Instanz-Speicher); leere Passphrase wird abgelehnt; falsche Passphrase → KdpCredentialError mit sprechender Meldung (GCM-Authentizitätsprüfung). getAccessToken(): Env-Override KDP_API_KEY gewinnt (CI/CLI-Pfad ohne Store), sonst entschlüsselter Refresh-Token, sonst null (graceful). IO über CredentialKV-Interface injizierbar (Settings-Store in der App, MEMORY_CREDENTIAL_STORE für Tests/CLI ohne Tauri). | Interface-Impact: ja (additiv) | Tests: 10 (Chiffretext-Rohwert, Roundtrip über Store-Neustart, falsche Passphrase, Env-Override, remove/has, leere Passphrase)

[AGENT-1] [src/services/bookwriter/kdpUploadTracker.ts + kdpUploadTracker.test.ts] NEU (Teil 3 — Upload-Status-Tracking): Status-Maschine uploading/processing/live/rejected (+idle) mit erlaubten Übergängen als Tabelle (idle→uploading/rejected, uploading→processing/rejected, processing→live/rejected; live/rejected terminal); verbotene Übergänge werfen KdpUploadTransitionError. transitionUpload(state, to, opts) pure: History-Audit-Trail je Statuswechsel, Fortschritt geklemmt 0–100, Zeitstempel startedAt/finishedAt/updatedAt; uploading→uploading dient als Fortschritts-Update ohne History-Spam; Transition auf live setzt Fortschritt automatisch auf 100 %. createUploadTracker() als Observable (subscribe/unsubscribe/setState) für CLI UND Dashboard-GUI; renderUploadProgress(state) als reine Render-Funktion (Status-Label deutsch, 10-Segment-Balken, Prozent, Ablehnungsgrund, Dauer) — dashboardspezifische Labels/Farben als UPLOAD_STATUS_LABELS/UPLOAD_STATUS_COLORS (Konsistenz mit progress.ts-Muster). | Interface-Impact: ja (additiv) | Tests: 14 (alle legalen/illegalen Übergänge, History, Klemmung, Observer, Render inkl. rejected-Grund und 100 % bei live)

[AGENT-1] [src/services/bookwriter/kdpUploadValidation.ts + kdpUploadValidation.test.ts] NEU (Teil 4 — Pre-Upload-Validierung): validateUploadArtefact(file, metadata, {isbn}) prüft (a) Datei vorhanden + nicht leer + ≥1 KB + ≤650 MB (KDP-Limit, lesbare MB-Angabe in der Meldung via formatBytes), (b) Format DOCX/EPUB (Extension + MIME-Konsistenz als Warnung), (c) KDP-Pflichtfelder (Titel, Klappentext, ≥1 Keyword; Preis 0.99–200 USD als Fehler, fehlender Preis nur Warnung), (d) ISBN-13-Prüfziffer via bestehendem isValidIsbn13 aus kdp/uploadSheet (optional — KDP vergibt alternativ eigene). Ergebnis strukturiert: issues[] {field, severity, message}, isValid, errorCount, warningCount. Reine Funktion, kein IO. | Interface-Impact: ja (additiv) | Tests: 13 (Format-Ablehnung PDF/MD/TXT, Größenlimits beidseitig, Pflichtfelder, Preis-Grenzen, ISBN valide/invalide, Error/Warning-Zählung)

[AGENT-1] [LOG] Akzeptanzkriterien Sprint 7 Agent 1: (1) KDP-Upload-Service existiert ✓ — src/services/bookwriter/kdpUpload.ts mit prepareUpload/buildUploadPackage/uploadToKdp/createKdpUploadService (Upload-Paket + injizierbarer Transport, da KDP keine öffentliche Self-Publisher-Upload-API bietet). (2) Credentials werden sicher gespeichert ✓ — AES-256-GCM (AWS1) über bestehende crypto.ts-Infrastruktur, Master-Passphrase nie persistiert, Env-Override KDP_API_KEY für CI; per Test: Rohwert im Store ist Chiffretext ohne Klartext-Substrings. (3) Upload-Status wird getrackt ✓ — Status-Maschine uploading/processing/live/rejected mit Transition-Guards, Audit-History, Fortschritt 0–100, Observable-Tracker für CLI (renderUploadProgress-Zeile) und Dashboard (Labels/Farben analog progress.ts). Verifikation: 48/48 neue Tests grün (4 Dateien); Regression src/services/kdp + bookwriter/progress + workflow.e2e 112/112 grün; tsc --noEmit clean für alle 8 eigenen Dateien (verbleibende tsc-Fehler in prompts/styles.test.ts sind vorbestehend aus parallel laufenden, uncommitteten Änderungen eines anderen Agenten an library.ts/prompts.json — nicht von Sprint-7-Agent-1 berührt); eslint clean (alle 8 Dateien). Budget: 0 echte API-Calls (alle Transporte über Fakes). Keine Breaking Changes (nur neue Dateien, keine Bestands-Signaturen geändert).

## Sprint 7 — Agent 3: Stil/Ton-Presets

[AGENT-3] [src/services/bookwriter/prompts/prompts.json + prompts.schema.json] ERWEITERT (Teil 1 — Daten): Neues top-level `styles`-Array mit 5 vordefinierten Stil/Ton-Presets: wissenschaftlich (präzise, zitierfähig, distanziert), blog (nah, direkt, Storytelling), jerry-cotton (kurz, szenisch, 1950er-Pulp), sachbuch-klassisch (strukturiert, Beispiele, Fußnoten [^n]), thriller (Spannung, Cliffhanger, kurze Sätze). Je Preset { id, label, description, systemHint, rules[≥2] }; version bleibt 2.0 (additive Erweiterung). JSON-Schema (Draft-07) um optionales `styles` erweitert — Libraries ohne styles bleiben valid (Abwärtskompatibilität). | Interface-Impact: ja (neue Daten + Schema-Feld, additiv) | Tests: via styles.test.ts

[AGENT-3] [src/services/bookwriter/prompts/library.ts + ../prompts.ts] ERWEITERT (Teil 2 — Stil-Mapping): StylePreset-Interface, listStyles() (Reihenfolge wie in prompts.json), getStyle() (case-insensitive, unknown/leer → null), styleOverlay() (rendert „Stil-Overlay: <id>“ + systemHint + „Stil-Regeln:“ als eigene Regelzeilen). systemFromProfile()/systemForGenre() um OPTIONALen 4. Parameter `style` erweitert: bekanntes Preset → Overlay wird ANGENÄHLT (Genre-Role/-Regeln bleiben unverändert am Anfang); unbekanntes/leeres Stil-ID → byte-identisch zum Sprint-6-Stand. Alle Bestands-Caller mit 3 Argumenten unverändert → keine Breaking Changes. | Interface-Impact: ja (additiv: neuer optionaler Parameter + 3 Exports) | Tests: 14 neue (styles.test.ts: 5 Presets, Byte-Identität ohne Stil, Overlay-Injektion, Regelreihenfolge, Fallback bei unbekanntem Stil, Fassade-Durchreichung, Briefing-tone-Semantik Preset-ID vs. freier Text)

[AGENT-3] [src/components/Writing/BookWriterPanel.tsx + BookWriterPanel.styles.test.tsx] ERWEITERT/NEU (Teil 3 — Stil-UI): Freies Stil-Textfeld in BEIDEN Ansichten (planner + classic) durch Dropdown ersetzt: „Kein Stil-Preset“ + die 5 Presets aus prompts.json (listStyles(), keine harte Kopie). Gewählte Preset-Beschreibung wird als <small> unter dem Dropdown angezeigt (Transparenz). Auswahl setzt tone auf die Preset-ID; config.tone/Jobs unverändert → Preset fließt über briefing.tone in die Generierung. | Interface-Impact: ja (GUI: input → select; Job-Config-Schema unverändert) | Tests: 3 neue (Dropdown statt Textfeld mit exakt 6 Optionen, Beschreibungs-Anzeige, Dropdown auch im klassischen Modus)

[AGENT-3] [src/services/bookwriter/chapter-gen.ts + workflow.ts] ERWEITERT (Teil 4 — Verdrahtung): Alle 7 systemForGenre-Call-Sites reichen briefing.tone als Stil-Kandidat durch — getStyle(briefing.tone) entscheidet: Preset-ID (aus dem GUI-Dropdown) → Stil-Overlay im System-Prompt; freier Text (Legacy-Briefings, z.B. „düster“) → null → Verhalten exakt wie Sprint 6. Damit ist das Akzeptanzkriterium „Stil wird in Prompts injiziert“ Ende-zu-Ende erfüllt. | Interface-Impact: nein (Signaturen unverändert, nur Argument-Weiterreichung) | Tests: abgedeckt durch styles.test.ts + Bestandsregression

[AGENT-3] [LOG] Akzeptanzkriterien Sprint 7 Agent 3: (1) 5 Stil-Presets in prompts.json ✓ — wissenschaftlich, blog, jerry-cotton, sachbuch-klassisch, thriller (je label/description/systemHint/rules, Schema validiert, version 2.0 unverändert). (2) Dropdown in GUI statt Textfeld ✓ — BookWriterPanel planner+classic: select mit „Kein Stil-Preset“ + 5 Presets, Beschreibung sichtbar, Auswahl → tone → Job-Config. (3) Stil wird in Prompts injiziert ✓ — systemForGenre(4. Parameter) hängt Stil-Overlay (systemHint + Regeln) an den Genre-System-Prompt an; chapter-gen/workflow reichen briefing.tone als Preset-ID durch (Legacy-Freitext → kein Overlay, byte-identisches Altverhalten). Verifikation: styles.test.ts 14/14, BookWriterPanel.styles.test.tsx 3/3, prompts/-Regression 27/27, chapter-gen 12/12, Writing-Panel-Regression 12/12; tsc --noEmit clean (exit 0), eslint clean (alle 7 geänderten Dateien). Budget: 0 echte API-Calls (alle Tests offline, LLM gemockt). Keine Breaking Changes (optionaler Parameter, additiver Daten-Feld, Select statt Input bei sonst identischem Verhalten).

## Sprint 6 — Agent 1: Telemetrie & Token-Analytics

[AGENT-1] [src/services/cli/costCalculator.ts + stats.ts + statsCommand.ts (+ Tests)] NEU: (1) Kosten-Rechner — callCostUsd(meta) = tokens_est/1e6 × priceFor(model), Preisliste MODEL_PRICES (deepseek-chat 0.5, deepseek-r1 1.0, gpt-4o-mini 1.0, claude-3.5-haiku 1.5, llama-3.1-8b 0.1 USD/1M, unbekannt → DEFAULT_CLOUD_PRICE_PER_M 1.5), lokale Provider (ollama/lmstudio/local/mock) = 0; counterfactualCloudCostUsd für die Ersparnis-Rechnung; computeCostReport(calls) mit Totals (Tokens, reale Cloud-Kosten, potenzielle Cloud-Kosten, Ersparnis = potenziell − real). (2) collectStats()/computeJobStats(): liest bookwriter_jobs (+ telemetry_json, Migration 020), Dauer = updated_at − created_at, Bucket local/cloud (cloud ab erstem Cloud-Call), Provider-Verteilung (Calls/Tokens/ø-Latenz/Fallbacks/Fehler), Zeit-pro-Buckel mit ø Dauer pro Buch; renderStats() als textuelles Dashboard mit graceful leerem Bestand. (3) CLI `--stats` — parseStatsArg/runStatsCommand als dünner Adapter, cli.ts main() steigt nach dem Stats-Druck aus (kein readline-Loop); Log-Eintrag via logger.info (context cli/stats). | Interface-Impact: ja (additiv: neue Module + --stats-Flag, Bestands-CLI-Pfad unverändert) | Tests: 17 (stats 12, statsCommand 5)

[AGENT-1] [statsCommand.ts FIX nach Real-Run + Tests erweitert] Review-Fund: `--stats` warf außerhalb von Vitest „DB nicht initialisiert" — main() führte den Befehl aus, bevor eine DB existierte (die App-DB wird nur im Tauri-Webview via initDb() geladen). Fix: ensureStatsDb() lädt die bestehende App-DB (%APPDATA%\com.aiwriterstudio.app\user_data\app.db) per createRequire (bundler-sicher, kein „Dynamic require" im ESM-Bundle), wendet Migrationen an und stellt sie als __aws_db bereit — bewusst NUR LESEND (kein persist(), Datei bleibt unangetastet; geprüft per Test: keine Seitendateien neben der gelesenen DB). Ohne DB-Datei (frische Installation) → leere, migrierte In-Memory-DB → graceful „Keine historischen Daten". runStatsCommand() jetzt async, cli.ts awaited. | Interface-Impact: ja (additiv: ensureStatsDb/defaultAppDbPath) | Tests: +5 (Pfad-Bau, No-op bei injizierter DB, leere-DB-Fallback mit Migrations-Check, Datei-Lesen nur-lesend, async Druck) → 22/22

[AGENT-1] [LOG] Akzeptanzkriterien Sprint 6 Agent 1: (1) `stats`-Befehl zeigt historische Daten ✓ — E2E real ausgeführt (esbuild-Bundle dist/cli.mjs + node): App-DB mit 3 abgeschlossenen Jobs (2 lokal ollama, 1 Cloud openrouter) gelesen, Dashboard zeigt Job-Historie, Token-/Kapitel-Totals, Provider-Verteilung und Zeit-pro-Buch-Metrik (Lokal ø 22.5 min/Buch, Cloud ø 7.0 min/Buch). (2) Kosten-Rechner ✓ — reale Cloud-Kosten $0.0210 vs. Counterfactual, Ersparnis durch lokales Routing $0.1230 (285% Ersparnis im Seed-Szenario). (3) Zeit-Pro-Buch-Metrik ✓ — separiert lokal/cloud mit ø Dauer, ø Call-Latenz und Token-Summen, im Dashboard gerendert. scripts/seed-stats-e2e.mjs (esbuild-Inline-Bundle) reproduziert das Szenario. Verifikation: stats.test.ts + statsCommand.test.ts 22/22, src/services/cli gesamt 91/91, Telemetrie/Jobs-Regression 16/16, eslint clean (alle geänderten Dateien). Hinweis: die Produktions-DB des Users (Sep 3) stammt aus der Zeit vor Sprint 4 (bookwriter_runs, noch ohne bookwriter_jobs) → dort rendert `--stats` korrekt „Keine historischen Daten". Budget: 0 echte API-Calls. Keine Breaking Changes.

## Sprint 6 — Agent 2: Prompt Library & Genre-Spezialisierung (Template-System)

[AGENT-2] [src/services/bookwriter/prompts/template.ts + template.test.ts] NEU (Teil 1 — Handlebars-Template-Engine): Minimale Handlebars-kompatible Engine BEWUSST OHNE handlebars-Dependency (Konvention wie Mini-YAML-Parser in styleProfiles.ts, Sprint 2 — Abhängigkeitsfläche des Desktop-Produkts bleibt klein). Unterstützte Teilmenge: {{var}}/{{pfad.wert}}-Substitution (ROH, kein HTML-Escaping — Prompts sind Klartext, Entities würden deutsche Anführungszeichen verfälschen), {{this}}, {{@index}}/{{@index1}} (0-/1-basiert, @index1 als Erweiterung für "Kapitel N:"-Zeilen), {{@first}}/{{@last}}, {{#if}}/{{else}}, {{#unless}}, {{#each}} mit Scope-Kette (innerer each-Scope verdeckt äußere Variablen nicht). Handlebars-Truthiness: false/null/undefined/""/[] falsy, 0 truthy; fehlende Variablen → leerer String. Sprechende Template-Fehler ({{#if}} ohne {{/if}} etc.). Reine Logik, keine IO. | Interface-Impact: ja (neues Modul, additiv) | Tests: 20 neue (Variablen, Pfade, alle Blöcke, Truthiness, kein-Escaping, Fehlerfälle)

[AGENT-2] [src/services/bookwriter/prompts/prompts.json + prompts.schema.json] NEU (Teil 2 — Externe Prompt-Library): ALLE 9 User-Prompt-Templates (titles, subtitles, positioning, outline, writeChapter, summarizeChapter, blurb, keywords, qualityCheck) als Handlebars-Templates + System-Prompts als strukturierte Genre-Profile { systemRole, systemRules[] } — 11 Genre-Profile: die 8 Legacy-Genres (byte-identische Roles aus dem bisherigen ROLE_BY_GENRE) + die 3 neuen Sprint-6-Spezialisierungen sachbuch-it (Fachbuchautor IT/Software, Codebeispiele, Grundkenntnisse), ratgeber-gesundheit (einfühlsam, belegte Erkenntnisse, keine Heilversprechen), fiction-thriller (Thriller-Role). Neue Genres sind damit reine Datenänderung in prompts.json — kein Code-Touch. JSON-Schema (Draft-07) liegt daneben; Validierung via eigener Checker + loadPromptLibraryOverride. | Interface-Impact: ja (neue Daten-Dateien, additiv) | Tests: via library.test.ts

[AGENT-2] [src/services/bookwriter/prompts/library.ts + ../prompts.ts ERWEITERT] NEU/ERWEITERT (Teil 3 — Fassade ohne Breaking Changes): library.ts lädt prompts.json zur Build-Zeit (Import, kein Runtime-IO) und bietet PROMPT_LIBRARY, listGenres(), listTemplates(), resolveGenre() (case-insensitive + Trim + Legacy-Fallback auf sachbuch, identisch zum bisherigen systemForGenre-Verhalten), normalizeGenreKey(), systemFromProfile() (rolle + regeln + sprachnotiz) und renderPrompt() (benanntes Template + Handlebars-Vars). prompts.ts bleibt Fassade mit UNVERÄNDERTEN Signaturen aller 10 Prompt-Funktionen + systemForGenre — die Funktionen rendern jetzt aus der Library; gerenderte Outputs sind byte-identisch zu den bisherigen Hardcoded-Strings (Byte-Identität per Test verifiziert, inkl. prev/research-Blockkombinationen in promptWriteChapter und 4000/3000-Zeichen-Caps). | Interface-Impact: nein (Signaturen unverändert; additiv: neue Exports) | Tests: 27 neue (library.test.ts)

[AGENT-2] [src/services/cli/promptArgs.ts + promptArgs.test.ts] NEU (Teil 4 — CLI-Flags): parsePromptArgs liest --genre= (Genre-Profil via resolveGenre), --audience= ({{targetAudience}}), --tone= (Tone-of-Voice, {{tone}}), --length=12x2500 (Buchlänge → chapterCount/wordsPerChapter) und --prompts= (Pfad-Override der Library, validiert vor dem Start). formatPromptFlags() für die CLI-Anzeige, loadPromptLibraryOverride() mit Validierung (version/genres/systemRole/systemRules/prompts je Genre, sprechende PromptLibraryError mit cause — Konvention wie ProviderError). cli.ts: main() zeigt Flag-Zeile und validiert Override vor dem Start (additiv, ohne Flags neutral). | Interface-Impact: ja (additiv: neue CLI-Flags + PromptLibraryError) | Tests: 17 neue (Parsing, --length-Fehlformat neutral, Validierungsfehler je Feld, gültige externe Library, eingebautes prompts.json besteht eigene Validierung)

[AGENT-2] [LOG] Akzeptanzkriterien Sprint 6 Agent 2: (1) Prompts in externer Datei ✓ — alle 9 Templates + System-Roles in prompts.json (58 KB, 11 Genres), Fassade enthält keinen Prompt-Text mehr (per Test sichergestellt). (2) Genre-Profile via Flag ladbar ✓ — --genre=sachbuch-it|ratgeber-gesundheit|fiction-thriller (+ 8 Legacy-Genres), resolveGenre case-insensitive, Unbekanntes fällt auf sachbuch zurück (Legacy-Verhalten). (3) Handlebars-Variablen funktionieren ✓ — targetAudience, tone, Buchlänge (chapterCount/wordsPerChapter/chapterWords) injiziert; @index1-Erweiterung für 1-basierte Kapitelnummerierung; fehlende Variablen leer (Handlebars-Verhalten). Smoke-Test real ausgeführt (esbuild-Bundle + node): Flags geparst, 11 Genre-Profile geladen, HB-Vars gerendert ("Zielgruppe: IT-Berufe | Umfang: 12 Kapitel, je ca. 2500 Wörter"). Budget: 0 echte API-Calls (alle Tests offline). Keine Breaking Changes (alle 10 Prompt-Funktionen mit unveränderten Signaturen, byte-identische Outputs, CLI ohne Flags neutral). | Tests: 67 neue (template 20 + library 27 + promptArgs 17 + cli-Wiring 3), Gesamtsuite 1629/1629 grün (143 Dateien), tsc clean, eslint clean (alle eigenen Dateien).

## Sprint 6 — Agent 3: Multilingual Pipeline (Automatische Übersetzung fertiger Manuskripte)

[AGENT-3] [src/services/bookwriter/multilingualPipeline.ts + multilingualPipeline.test.ts] NEU (Teil 1+2 — Translation-Service & Markup-Erhalt): Pipeline-Ebene über dem Sprint-3-TranslatorService — fertiges Buch (ChapterData-Artefakt des Manuskript-Phases) wird kapitelweise in EN/ES/FR übersetzt. TRANSLATION_TARGETS (en/„Englisch", es/„Spanisch", fr/„Französisch", erweiterbar via options.targets); translateBookToLanguages(chapters, chat, {targets?, sourceLanguage?, glossary?}, onProgress, signal) läuft je Sprache über das bestehende translateBook() — Markup-Erhaltung dadurch unverändert durch markupGuard (maskMarkup/restoreMarkup/markupIntact je Kapitel, ⟦M##⟧-Platzhalter); globaler Fortschritt über alle Sprachen (Kapitel×Sprachen), AbortSignal bricht zwischen Sprachen ab und liefert Teilstand. Jeder LLM-Call über die bestehende LLMChatFn-Abstraktion → provider-agnostisch, 0 echte API-Calls in Tests. estimateTranslationApiCalls(chapterCount, targets?) schätzt das Call-Budget (Kapitel×Sprachen + 1 Metadaten-Call je Sprache) vor dem Start. | Interface-Impact: ja (additiv: neue Exporte in bookwriter/index.ts, keine Änderung an Bestands-Signaturen) | Tests: 10 neue (EN/ES/FR-Vollübersetzung mit Markup-Assertion Headings/Bold/Listen, Fortschritt 1/6..6/6, Teilmenge, Abort-Teilstand, Glossar-/Quellsprache-Weitergabe)
[AGENT-3] [multilingualPipeline.ts, Teil 3 — KDP-Metadaten-Lokalisierung]: translateKdpMetadata(metadata, chat, target, signal?) lokalisiert Titel/Untertitel/Klappentext/Kurzbeschreibung/Keywords in EINEM JSON-Call je Sprache (buildMetadataTranslationPrompt exponiert für Tests: JSON-Vertrag, Keyword-Limits im Prompt). Robustes JSON-Extraktion (Klartext + ```json-Fences), KDP-Limits erzwungen: max. 7 Keywords (KDP-Limit), >50-Zeichen-Keywords verworfen, beides als warnings gemeldet; ungültige/leere Antworten → deterministischer Fallback (Original-Metadaten unverändert, viaLlm=false, Warning); Chat-Fehler → ebenfalls Fallback (kein Pipeline-Abbruch), Abort → sauberer AbortError. translateKdpMetadataToLanguages() lokalisiert sequentiell für alle Zielsprachen. buildLocalizedUploadSheet(sourceRow, translations): KDP-Spreadsheet-Lokalisierung — eine Zeile pro Sprache (Quellsprache zuerst, dann en/es/fr), nutzt unverändert buildKdpUploadSheet (RFC-4180, 20-Spalten-Vertrag); Title/Subtitle/Description/Keywords/Language aus den Lokalisierungen, ISBNs/Preise/Kategorie/Autor unangetastet, HTML-Klappentext via bestehendes toHtmlDescription, Determinismus garantiert. runMultilingualPipeline() führt Buch-Übersetzung + Metadaten-Lokalisierung in einem Aufruf mit Gesamt-Fortschritt aus. | Interface-Impact: ja (additiv) | Tests: 8 neue (JSON-Vertrag, Fences, Keyword-Kappung/Längenlimit mit Warnings, Fallback, Abort, 3-Sprachen-Metadaten, Sheet-Zeilen-pro-Sprache mit Language-Spalte en/es + HTML/ISBN/Preis-Erhalt, Determinismus)
[AGENT-3] [src/services/bookwriter/index.ts] EXPORT ergänzt: multilingualPipeline (public API des Bookwriter-Moduls). | Interface-Impact: ja (additiv) | Tests: —
[AGENT-3] [LOG] Akzeptanzkriterien Sprint 6 Agent 3: (1) Übersetzung fertiger Bücher in EN, ES, FR kapitelweise ✓ (10 Tests, je Kapitel markupIntact-Verifikation, Fortschritt + Abort + Glossar geprüft). (2) Markdown-/HTML-Markup bleibt erhalten ✓ (delegiert an bewährten markupGuard — Heading/Bold/Link/Liste/HTML-Block-Assertions in der EN/ES/FR-End-to-End-Übersetzung, Struktur-Verifikation je Kapitel). (3) KDP-Metadaten (Klappentext, Keywords) werden lokalisiert ✓ (8 Tests, 7-Keyword-Limit + 50-Zeichen-Limit erzwungen, lokalisiertes Upload-Sheet mit einer Zeile je Sprache). Budget: 0 echte API-Calls (alle Calls über LLMChatFn-Fakes); estimateTranslationApiCalls deckelt das reale Budget planbar (z.B. 10 Kapitel → 33 Calls). Keine Breaking Changes (nur additive Module + Exporte; translatorService/uploadSheet unangetastet). | Tests: 18 neue (multilingualPipeline.test.ts), Regressions-Check translatorService + uploadSheet 21/21 grün; Gesamtsuite 1520/1522 (2 Fehler in fremden uncommitteten Dateien logManager.test.ts — vorbestehend, keine meiner Dateien); tsc clean für alle 3 von mir berührten Dateien; eslint clean.

## Sprint 4 — Agent 1: Orchestrierung & CLI-Interface

[AGENT-1] [src/services/cli/dashboard.ts + dashboard.test.ts] NEU (Teil 1 — CLI-Dashboard): Interaktives Terminal-Interface für den Vollautomatik-Lauf. Reine Render-Logik (DashboardState → String, vollständig testbar): createDashboard/upsertAgent/updateAgentProgress/setAgentStatus/recordTokens (alle pure, requireAgent wirft bei unbekannter Id), progressBar (10 Segmente, proportional), renderDashboard mit Statusmarkern (▸ läuft, ✓ fertig, ✗ Fehler, · idle), [LOKAL]/[CLOUD]-Tags je Modell, Fehlerzeile ↳, Gesamt-Token-Zeile. Bewusst OHNE ink/inquirer-Abhängigkeit (keine neue Dependency); ANSI-Clear + readline nur im thin Entry-Point cli.ts. | Interface-Impact: ja (neues Modul src/services/cli/, additiv) | Tests: 8 neue

[AGENT-1] [src/services/cli/jobRecovery.ts + jobRecovery.test.ts] NEU (Teil 2 — Job-Recovery UI): findInterruptedJobs() — projektübergreifende Übersicht ALLER fortsetzbaren Jobs (status running|interrupted UND current_chapter>0, jüngster zuerst; JOIN projects für Fallback-Titel; identische Semantik wie getResumableBookJob aus Sprint 2). formatRecoveryPrompt liefert den geforderten Prompt: 'Möchten Sie das Buchprojekt "[Titel]" bei Kapitel X fortsetzen?' (Titel aus der Outline, sonst Projektname; X = current_chapter+1; Fortschritt X/Total angehängt). buildRecoveryChoice(info, 'resume'|'discard') — resume liefert startChapter=current+1 + jobId, discard löscht den Job via deleteBookJob (Kapitel bleiben erhalten). Robust ohne initialisierte DB (nackter CLI-Start → leere Liste statt Throw). | Interface-Impact: ja (neue Service-API, additiv; nutzt bestehenden Job-Store unverändert) | Tests: 9 neue (In-Memory-SQLite)

[AGENT-1] [src/services/cli/healthMonitor.ts + healthMonitor.test.ts] NEU (Teil 3 — CORS-Health-Monitor): Visuelle Ampel beim Start — checkInstanceHealth(name, url, timeoutMs=3000) prüft Erreichbarkeit (GET mit AbortController-Timeout) und kombiniert sie mit der CORS-Logik aus Sprint 3: grün = erreichbar UND CORS ok, gelb = 403 (CORS blockiert → OLLAMA_ORIGINS-Hinweis), rot = nicht erreichbar/HTTP-Fehlerstatus. checkAllLocalInstances() prüft DEFAULT_LOCAL_ENDPOINTS (Ollama :11434, LM Studio :1234, Hermes Agent :8080) parallel; renderTrafficLights() liefert die Ampelübersicht. | Interface-Impact: ja (neue Service-API, additiv) | Tests: 7 neue (fetch gemockt, keine echten Netzwerk-Calls)

[AGENT-1] [src/services/cli/cli.ts] NEU (Teil 4 — Einstiegspunkt): Dünner Terminal-Adapter (node:readline/promises, reines ANSI): main() = 1. runHealthCheck (Ampel), 2. runJobRecovery (interaktiver Prompt je fortsetzbarem Job, [j/N] → buildRecoveryChoice), 3. runDashboardLoop (Live-Refresh mit ANSI clear+home; Produktions-Verdrahtung: BookwriterRouter.onCall → recordTokens, Workflow-Kapitel fertig → updateAgentProgress). Smoke-Test real ausgeführt (esbuild-Bundle): Ampel zeigt Ollama lokal grün, LM Studio/Hermes rot (nicht gestartet), Dashboard rendert Fortschritt 1/8 + 1500 Tokens + Tags. | Interface-Impact: ja (additiv)

[LOG] Akzeptanzkriterien Sprint 4 Agent 1: (1) CLI zeigt Live-Fortschritt, Token-Verbrauch, Modell-Status — 8 Tests. (2) Job-Recovery erkennt abgebrochene Jobs und bietet Fortsetzung an — 9 Tests (interrupted+running erkannt, current_chapter=0/completed/aborted ignoriert, Sortierung, Prompt-Format, resume→current+1, discard löscht). (3) CORS-Health-Monitor zeigt Ampel grün/gelb/rot — 7 Tests (200→grün, 403→gelb mit OLLAMA_ORIGINS, nicht erreichbar→rot, leere Liste). Gesamtsuite: 1370/1370 Tests grün (123 Dateien, inkl. 24 neue CLI-Tests), tsc clean (cli/), eslint clean (cli/). Budget: 0 API-Calls. Keine Breaking Changes (nur neues Verzeichnis src/services/cli/).


## Sprint 3 — Agent 3: Advanced Publishing & Workflow-Integration

[AGENT-3] [src/services/bookwriter/export/docx.ts + vba.ts] NEU/ERWEITERT: DOCX-Export mit standardisierten Formatvorlagen — paragraphStyles {Standard (Fließtext, Georgia 12pt, Zeilenabstand 1.15), StandardEingerückt (Erstzeilen-Einzug 0.3", KDP-Prosa-Konvention), Einzug (links 0.5" + Erstzeile, für Zitate/Listen), Heading1, Heading2 (mit outlineLevel 0/1 → Navigationsfenster/Scrivener-Import)}; alle Absätze über pStyle statt Inline-Formatierung (verlustfreier Import in LibreOffice + Scrivener 3 über File > Import > Files). Core Properties (Titel/Autor/Keywords/Description) gesetzt. VBA-Cleanup-Prep: docProps/custom.xml mit Custom Properties {AIWS_AISuite="AI Text Refinement Suites", AIWS_Generator, AIWS_Version, AIWS_ChapterCount, AIWS_ExportedAt} + Custom XML Part customXml/item1.xml (Namespace urn:ai-writer-studio:ai-text-refinement; Buch-Knoten + je Kapitel index/title/status/hiddenTag + Kapiteltext; versteckte Tags = Zero-Width-Marker U+200B + Base36-Index, aiwsHiddenTagFor(n)) + customXml/itemProps1.xml (ds:datastoreItem, itemID GUID) + [Content_Types].xml-Overrides. VBA-Zugriff: ActiveDocument.CustomDocumentProperties("AIWS_AISuite") bzw. ActiveDocument.CustomXMLParts.SelectNodes("/*[local-name()='aiws']"). Implementierung: docx-Paket kann keine customXml-Parts → ZIP-Nachrüstung via JSZip (bereits Dependency, kein neues Paket). | Interface-Impact: nein (buildBookDocxBlob-Signatur unverändert, meta optional um language erweitert) | Tests: proworkflow.test.ts (5 DOCX-Tests: Style-Definitionen inkl. w:firstLine, pStyle-Verweise für Standard/Einzug/Heading1/Heading2, Core-Props, Custom-Props, Custom-XML-Part inkl. Hidden-Tag + Content-Types)

[AGENT-3] [src/services/bookwriter/export/opml.ts] NEU: buildBookOpml() — OPML 2.0-Outline für Scrivener 3 (File > Import > OPML): Wurzel-Outline = Buch, je Kapitel outline text="Kapitel N: Titel" mit Metadaten-Attributen _title (Roh-Titel ohne Präfix), _chapterNumber, _status (draft/completed/needs_revision); ownerName = Autor, dateCreated = ISO-Zeitstempel; XML-Escape über vba.xmlEscape. | Interface-Impact: ja (additiv) — ExportFormat = "markdown" | "docx" | "epub" | "opml"; exportBook(input, "opml") liefert { filename: "*.opml", blob, format }; saveExportBlob akzeptiert "opml" mit Scrivener-Dialogfilter; BookWriterPanel-Formatwahl um "Scrivener-Outline (.opml)" erweitert | Tests: proworkflow.test.ts (2 OPML-Tests: Struktur + Escape/Metadaten), BookWriterPanel.test.tsx aktualisiert (4 Formate)

[AGENT-3] [src/services/bookwriter/export/structure.ts + epub.ts] ERWEITERT: Jutoh-optimierter EPUB-Export — sauberes, semantisches HTML ohne Inline-Styles: Kapitelüberschrift <h1 id class="chapter-title">, erster Absatz je Kapitel class="noindent" (Buchsatz), Titelblatt zentriert über div.center, alle Formatierung in zentralem styles.css (neue Regeln .noindent, .chapter-title, .center; h1.chapter-title ohne page-break im Fließtext); OPF um <meta name="generator" content="AI Writer Studio (Jutoh-optimierter Export)"/> ergänzt. Jutoh mappt die Klassen beim Import 1:1 auf eigene Stile. | Interface-Impact: nein (buildEpubChapterXhtml/buildEpubTitleXhtml-Signaturen unverändert) | Tests: proworkflow.test.ts (3 EPUB-Tests: kein 'style="' in keiner XHTML-Datei des Pakets, semantische Tags + CSS-Klassen, Generator-Metadatum)

[AGENT-3] [src/services/bookwriter/export/artifacts.test.ts] ERWEITERT: Akzeptanznachweis auf Platte — 4. Artefakt .opml geschrieben + DOCX-Artefakt real verifiziert (customXml/item1.xml mit Namespace + Suite-Name + U+200B-Hidden-Tag, docProps/custom.xml mit AIWS_AISuite), EPUB-XHTML well-formed ohne Inline-Styles (geprüft via Python/zipfile + minidom nach Testlauf). | Interface-Impact: nein

Akzeptanzkriterien Agent 3: (1) DOCX mit standardisierten Formatvorlagen H1/H2/Standard/Einzug ✓ (w:styleId in styles.xml + pStyle-Verweise in document.xml) | (2) OPML-Export für Scrivener-Import ✓ (OPML 2.0, Kapitel-Metadaten, UI-Option, Save-Filter) | (3) EPUB ohne Inline-Styles, semantisches HTML ✓ (0 'style='-Attribute, CSS-Klassen im zentralen styles.css) | (4) DOCX mit Custom XML Parts für VBA-Makros ✓ (item1.xml + itemProps1.xml + Content-Types-Overrides + Custom Properties + versteckte Kapitel-Tags). Regressionsprüfung: bookwriter/export-Suite + BookWriterPanel 44/44 grün, tsc --noEmit sauber (mein Scope), eslint clean; Gesamtsuite 1266/1267 grün (2 Fail-Dateien translatorService.test.ts/probe.debug.test.ts = untracked Scope eines parallelen Agenten, nicht berührt).

[AGENT-2] [src/services/writing/bookwriter.ts] B1 Rolling Context: buildChapterContext() (Outline kompakt + chapterSummaries[0..N-1] + letzter Absatz des Vorkapitels + Glossar) ersetzt Vollkontext; summarizeChapter() (eigener Call, max_tokens 400, Ziel 150-250 Wörter) füllt outline.chapterSummaries[] nach jedem Kapitel. B2 Glossar: extractEntities() nach Kapitel 2 (Personen/Fachbegriffe/Zahlen, max. 30), mergeEntities() dedupliziert case-insensitive und cappt bei 30; liegt in outline.entities[]. B3: evaluateWordCount() mit deriveMinMax ±20%; bei Abweichung EIN adjustChapterWordCount()-Call (anhängen bei zu kurz / ersetzen bei zu lang), danach Status "needs_revision" statt blind weiter. B4: validateOutline() (Kapitelanzahl, Nummern, eindeutige Titel, Summaries >= 20 Wörter, logischer Bogen: kein Fazit als Kapitel 1, max. 1 Fazit); bei Verletzung EIN repairOutline()-Call, danach Fehler mit Hinweis auf manuellen Eingriff. | Interface-Impact: JA — BookOutline + chapterSummaries?: string[], BookOutline + entities?: string[], BookChapter + status?: "draft" | "needs_revision", BookWriterConfig + wordsPerChapter?: number (alle optional, kein Breaking Change; generateOutline/generateChapter/generateBook-Signaturen unverändert) | Tests: 1035/1036 grün (24 neue; 1 Fail in BookWriterPanel.test.tsx = Scope des parallelen Agent-Workstreams A)

[AGENT-2] [HINWEIS] Parallel-Workstream-Koexistenz: Der Agent-Workstream A (A1/A2: Retry, JSON-Extraktion, BookWriter-Jobs) hat auf derselben bookwriter.ts aufgebaut (parseJsonLoose, withRetry, validateChapterShape). Der Commit enthält deshalb zusätzlich dessen abhängige Module src/services/writing/jsonExtract.ts und src/services/writing/retry.ts als Kompilier-Abhängigkeiten. Verbleibende WIP-Dateien (chapterEngine.ts, BookWriterPanel.tsx, projectStore.ts, migrations, jobs.ts, Panel-Tests) sind NICHT im Commit und gehören zu Workstream A; dort 1 roter UI-Test + 1 tsc-Fehler (jsonExtract.test.ts) im verantwortlichen Scope A.

[AGENT-2] [src/services/writing/bookwriter.quality.test.ts] NEU: Logik-Tests für B1 (Kontextaufbau, lastParagraph, Token-Budget < 4000 für Kapitel 8 bei 8x1000 Wörtern), B2 (Dedup/Cap/leere Einträge, Mock-Prompt enthält exakte Glossar-Bezeichnung → Namensdrift-Prävention), B3 (400 Wörter bei Ziel 1000 → außerhalb ±20%, Grenzfälle 799/800/1200/1201), B4 (alle Gate-Regeln inkl. doppelter Titel, kurzer Summaries, Fazit-Bogen). | Interface-Impact: nein | Tests: 983/983 grün (18 neue)

[AGENT-2] [src/services/writing/bookwriter.generation.test.ts] NEU: generateChapter() mit gemocktem OllamaProvider: Prompt enthält Glossar + Übergangsabsatz statt Vollkontext; 400-Wörter-Kapitel bei Ziel 1000 löst genau EINEN Nachsteuer-Call aus; Nachsteuer erfolgreich → draft (400+600=1000 bzw. 400+450=850), Nachsteuer unzureichend → needs_revision (400+100=500); 1500 Wörter → Kürzungs-Call mit Ersetzung → draft; Kapitel innerhalb ±20% → kein Nachsteuer-Call. | Interface-Impact: nein | Tests: 983/983 grün (6 neue)

[AGENT-1] [src/services/writing/jsonExtract.ts] NEU (A1): Zweistufige JSON-Extraktion ersetzt reines Bracket-Counting: (a) stripFences + direkter JSON.parse, (b) extractJsonObject-Zustandsmaschine (inString/escaped — `}` im String bricht nicht vorzeitig ab), (c) repairJson (trailing commas, einfache Anführungszeichen — Apostrophe im String bleiben Inhalt) + capTruncatedJson (abgeschnittenes JSON an letztem vollständigem Kapitel kappen). parseJsonLoose() wirft sprechende Fehler mit Antwort-Vorschau. bookwriter.ts: validateChapterShape() wirft 'Kapitel N fehlt summary/title'; generateOutline/repairOutline/extractEntities nutzen parseJsonLoose. | Interface-Impact: nein (interne Hilfsfunktionen; generateOutline/generateChapter-Signaturen unverändert, neuer optionaler Parameter timeoutMs=120000) | Tests: 1036/1036 grün (21 neue: jsonExtract.test.ts 15 + retry.test.ts Anteil)

[AGENT-1] [src/services/writing/retry.ts] NEU (A2/A3): withRetry() mit max. 3 Versuchen, Backoff 1s/3s/8s + ±20% Jitter, sleepWithAbort; Retry NUR bei timeout/network/json, KEIN Retry bei AbortError/4xx (classifyError/isRetryable/isAbortError); createTimeoutController() kombiniert externes Signal + Timeout zu einem Abbruch-Signal. bookwriter.ts: generateOutline/generateChapter laufen über withRetry + collectChat (kombiniertes Signal), bei wiederholtem JSON-Fehler schärferer Prompt (STRICT_JSON_SUFFIX: 'Antworte NUR mit validem JSON, kein Text davor oder danach'); generateBook fängt Abort ab und liefert sauberes Teilergebnis. chapterEngine.ts: generateChapterChunked/expandChapter nutzen konfigurierbares GenerationConfig.timeoutMs (Default 120000) + createTimeoutController; Chunk-Generation via withRetry. | Interface-Impact: JA — GenerationConfig + timeoutMs: number (optional via DEFAULT_CONFIG, kein Breaking Change); generateOutline/generateChapter + timeoutMs-Parameter (optional) | Tests: 1036/1036 grün (17 neue in retry.test.ts: Fake-Provider 2× Scheitern/3. Versuch Erfolg, kein Retry bei Abort/4xx, Strict-Prompt-Erfassung, Abort → Status planned, Timeout-Controller)

[AGENT-1] [src/services/writing/chapterEngine.ts] (A3) Abort-Semantik: Loop-Abort UND Catch-Path in generateChapterChunked setzen chapter.status auf 'planned' (vorher 'draft'/'needs_revision' Ghost-State) mit error 'Abgebrochen'. | Interface-Impact: nein | Tests: 1036/1036 grün

[AGENT-3] [src/services/db/migrations/018_bookwriter_jobs.ts] NEU Migration 018: Tabelle bookwriter_jobs (id, project_id, config_json, outline_json, status, current_chapter, error, created_at, updated_at, FK auf projects ON DELETE CASCADE, Indizes idx_bwj_project/idx_bwj_status) + Planungsspalten auf chapters (status, target_word_count, minimum_word_count, maximum_word_count, current_word_count, purpose, synopsis, last_error — idempotentes ALTER via PRAGMA table_info). Registriert in migrations/index.ts als Version 18 "bookwriter_jobs". | Interface-Impact: JA — DB-Schema +2 Artefakte (Tabelle bookwriter_jobs, 8 neue chapters-Spalten); kein Breaking Change (alle Spalten haben Defaults, Re-Runs idempotent) | Tests: 1036/1036 grün (8 neue in jobs.test.ts)

[AGENT-3] [src/services/bookwriter/jobs.ts] NEU Job-Store: createBookJob (persistNow beim Anlegen), setBookJobOutline, updateBookJobProgress (current_chapter je gespeichertem Kapitel, persistNow → committed Row pro Kapitel, kein Datenverlust bei Prozess-Kill), setBookJobStatus, loadBookJob, getResumableBookJob (status running/interrupted UND current_chapter>0), completeBookJob, deleteBookJob. | Interface-Impact: JA — neues Modul src/services/bookwriter/jobs.ts mit BookJob/BookJobStatus-Typen | Tests: 1036/1036 grün (8 neue)

[AGENT-3] [src/services/project/index.ts] updateChapterFields() NEU: persistiert Status/Wortzahlen/Planungs-/Fehlerfelder inkrementell (Whitelist-Spaltenset, sofort persist()). listChapters()/getChapter()/createChapter() lesen/schreiben jetzt die Migration-018-Spalten (status, target_word_count, minimum_word_count, maximum_word_count, current_word_count, purpose, synopsis, last_error) statt harter Defaults. | Interface-Impact: JA — Chapters zurückgeben jetzt echte Status-/Planungsfelder aus der DB (vorher konstant "planned"/2000/…; Felder waren bereits im Chapter-Typ optional vorhanden, Werte sind jetzt persisted) | Tests: 1036/1036 grün

[AGENT-3] [src/store/projectStore.ts] updateChapter() schreibt jetzt INKREMENTELL via updateChapterFields in SQLite (kein RAM-Puffer). updateChapterWordCount() delegiert auf updateChapter. reconcileOutline() NEU (C4): Gliederung neu generieren — behält fertige Kapitel (draft/completed), markiert betroffene (abweichender Titel) als needs_revision, plant neue Kapitel ein. reorderChapters() persistiert order_index. INTERFACE-CHANGE: ProjectState + reconcileOutline(newChapters) | Tests: 1036/1036 grün

[AGENT-3] [src/components/Writing/BookWriterPanel.tsx] C1: runGeneration() schreibt jedes fertig generierte Kapitel SOFT in Store (Status draft), Job-Fortschritt wird je Kapitel via updateBookJobProgress committet; Agent-1-Retry via withRetry (3 Versuche) mit Retry-Zähler im UI. C2: Beim Panel-Start prüft getResumableBookJob() auf interrupted/running-Jobs → Resume-Dialog ("Generierung fortsetzen?"); Resume startet bei current_chapter+1 und nutzt die gespeicherte Outline aus dem Job; Verwerfen löscht den Job, Kapitel bleiben. C3: Kapitel-Rows zeigen Status-Badge (STATUS_LABELS/Farben, Quelle identisch zu ChapterPlanner), Wortzahl vs. Ziel, Retry-Zähler (🔁 N× Retry), inline Fehler im Row mit "erneut versuchen?"-Button statt generischem Abbruch; Restzeit-Schätzung aus gleitendem Durchschnitt der letzten 3 Kapitelzeiten; Abort-Button bestätigt ("Bereits generierte Kapitel bleiben erhalten."). C4: Button "Gliederung neu generieren" ruft reconcileOutline() (nur Outline, fertige Kapitel bleiben). | Interface-Impact: ja (neue Props/Exports STATUS_LABELS/STATUS_COLORS; Verhalten: Resume statt Neustart) | Tests: 1036/1036 grün (7 neue in BookWriterPanel.test.tsx)

[AGENT-3] [src/components/Writing/ChapterPlanner.tsx] Inline-Fehler im Kapitel-Row (C3): ch.lastError wird als cp-chapter-error mit "erneut versuchen?"-Button angezeigt (setzt lastError zurück, Status auf planned) statt generischem Abbruch. | Interface-Impact: nein | Tests: 1036/1036 grün

[AGENT-3] [src/components/Writing/BookWriterPanel.test.tsx] NEU: Component-Tests (jsdom) für Resume-Dialog (erscheint bei interrupted-Job mit current_chapter>0), Resume startet bei current_chapter+1 (setBookJobStatus running), Verwerfen ruft deleteBookJob, Abort-Flow bestätigt, "Gliederung neu generieren"-Button vorhanden, Kapitel-Rows zeigen Status-Badges/Wortzahlen/Inline-Fehler mit "erneut versuchen?". | Interface-Impact: nein | Tests: 1036/1036 grün (7 neue)

[AGENT-3] [src/services/bookwriter/jobs.test.ts] NEU: Job-CRUD, Progress-Commit, Resume-Bedingungen (current_chapter>0, completed nicht fortsetzbar), deleteBookJob, FK-Kaskade Projekt→Jobs, Migration-018-Idempotenz + Planungsspalten-Check. | Interface-Impact: nein | Tests: 1036/1036 grün (8 neue)

[AGENT-3] [src/components/KIPanel/ki.css] CSS für bw-resume (Resume-Dialog), bw-chapter-meta (Badge/Wortzahl/Retry-Badge), bw-chapter-error/cp-chapter-error (inline Fehler), bw-retry-btn/cp-retry-btn. Keine Layout-Regression (nur additive Klassen im bestehenden Panel-Flow). | Interface-Impact: nein | Tests: 1036/1036 grün

## Sprint 2 — Agent 4: Redaktion & Revisions-Loop (needs_revision → produktiver Loop)

[AGENT-4] [src/services/db/migrations/019_revision.ts + index.ts] NEU: Migration 019 — Tabellen style_profiles (id, project_id nullable, name, system_hint, rules_json, is_preset, Timestamps) + chapter_revisions (id, chapter_id FK→chapters ON DELETE CASCADE, mode, model, before/after_words, before/after_filler, note, created_at). DB-CHANGE: 2 neue Tabellen + Indizes idx_style_profiles_project / idx_chapter_revisions_chapter; schema_migrations Version 19. | Interface-Impact: ja (DB-Schema) | Tests: migration019.test.ts (5 Tests: Tabellen, Spalten, Idempotenz, FK-Kaskade)

[AGENT-4] [src/services/writing/styleProfiles.ts] NEU: Stilprofile { id, name, systemHint, rules[] } pro Projekt + 3 Presets (Sachbuch klar, Ratgeber warm, Thriller temporeich), Seed idempotent bei leerem Bestand. CRUD (list/create/update/delete), Markdown-Import mit YAML-Frontmatter-Parser ohne yaml-Dependency (Skalar-Keys, "- Item"-Listen, Quoted Values, Kommentar-/Leerzeilen-Toleranz). | Interface-Impact: nein (additiv) | Tests: styleProfiles.test.ts (12 Tests inkl. Akzeptanzkriterium Import aus Markdown)

[AGENT-4] [src/services/writing/readability.ts] NEU: Lesbarkeits-Metriken ohne LLM — Flesch Reading Ease (deutsche Anpassung: 180 − ASL − 58,5·ASW), Ø-Satzlänge, Füllwort-Quote (deutsche Füllwort-/Floskelliste), Passiv-Schätzung (werden-Periphrase + Partizip II + worden). computeReadability(text, thresholds) mit konfigurierbaren Schwellen (DEFAULT_THRESHOLDS: Ø 18 W/S, Füll 8 %, Passiv 20 %, FRE 50), metricBadges(chapter) liefert Warn-Badges für UI. | Interface-Impact: nein | Tests: readability.test.ts (19 Tests)

[AGENT-4] [src/services/writing/revise.ts] NEU: reviseChapter(chapterId, mode, profile?, signal?) mit modes straffen (−10 % Ziel, Füllwort-Entfernung), vertiefen (+15 % Ziel, Beispiele), stil (Stilprofil → systemHint + rules im Prompt). LLM-Call via createProvider/buildMessages + withRetry (Sprint 1); Abort/4xx durchgereicht; straffen hat lokalen heuristischen Fallback (computeLocalTightening), wenn Provider nicht erreichbar. Nach Revision: content via updateChapter (verschlüsselt), Status → draft + currentWordCount via updateChapterFields, Revisionshistorie via saveRevision → chapter_revisions. listRevisions(chapterId) für UI. Budget-Warnung von Agent 2 konsumiert: installBudgetWarningListener/getBudgetWarning/clearBudgetWarning (Event bookwriter:budget-warning). | Interface-Impact: ja (neue Service-API; DB-CHANGE Revisionshistorie) | Tests: revise.test.ts (9 Tests: Füllwort-Quote 30 %→messbar gesenkt, Fallback, Stil-Prompt, Fehlerfälle, Historie-Stapel)

[AGENT-4] [src/components/Writing/ChapterReview.tsx] NEU: Review-UI — Kapitel-Liste mit Status-Badge (STATUS_LABELS/Farben identisch zu ChapterPlanner), Metrik-Badges (Ø Satzlänge/Füllwörter/Passiv/Flesch mit ⚠-Warnung), Aktionen Straffen/Vertiefen/Stil/✓ completed, Stilprofil-Select, aufklappbare Revisionshistorie (Wortzahl + Füllwort-Quote vorher→nachher), Budget-Warnung (Prop ODER Service-Event) sperrt alle LLM-Aktionen. Schwellenwerte-Anzeige umschaltbar. | Interface-Impact: ja (neue Komponente/Props) | Tests: ChapterReview.test.tsx (9 jsdom-Tests: Badges, Aktionen, Budget-Sperre, Historie, needs_revision-Zählung)

Akzeptanzkriterien: (1) 30 % Füllwörter → straffen senkt Quote ≥ 10 % relativ (Test sowohl mit Mock-LLM als auch lokal ohne Provider). (2) Stilprofil-Import aus Markdown mit geparstem Frontmatter (12 Tests). (3) needs_revision → draft per Revisions-Aktion, → completed per Freigabe-Button, Revisionshistorie in DB + UI sichtbar.

## Sprint 2 — Agent 1: Resilienz, FMEA & Red-Team (Test- und Risiko-Rückgrat)

[AGENT-1] [tests/helpers/fakeOllamaProvider.ts] NEU (A1): FakeOllamaProvider mit Skript-API — Sequenz von FakeSteps (good/brokenJson/timeout/abort/empty/huge/charChunks/throw), Chaos-Modi "timeouts" (jeder Call wirft nach delayMs TimeoutError) und "random" (deterministisch geseedet via mulberry32: 40 % gut, 10 % kaputtes JSON, 10 % Timeout, 10 % leer, 10 % Abort, 5 % Riesenantwort 300k Wörter, Rest Einzelzeichen-Chunks). Router-Modus (prompt → Antwort) für prompt-abhängige Skripte; exhaustedText-Fallback wiederholt letzten guten Text bei erschöpfter Warteschlange; calls[] erfasst prompt/model/options je Call für Assertionen; signal.aborted vor Call-Einstieg → sofortiger AbortError (Vertrags-Treue zum echten Fetch). goodOutlineJson(n) liefert gate-konforme n-Kapitel-Gliederung. Wird via vi.mock("@/services/llm/ollama") installiert — kein Netz, kein echter Ollama, CI-sicher. | Interface-Impact: nein (Test-Infrastruktur) | Tests: von Red-Team- + E2E-Suite konsumiert

[AGENT-1] [tests/bookwriter.redteam.test.ts] NEU (A3): 20 adversarielle Gliederungs-Antworten, jede Injection im Test dokumentiert (Vektor → erwartetes Verhalten → Endzustand): R01 Markdown-Fence-Wrapper (stripFences entschärft), R02 SQL-Payload in Feldern (bleibt Daten, parametrisierte DB), R03 Prompt-Injection "IGNORE ALL PREVIOUS INSTRUCTIONS" (Text bleibt Daten, ≤ 2 Calls), R04 falsche Kapitelzahl 5→3 (B4-Gate + Reparatur-Call), R05 doppelte Kapitelnummer, R06 Nummern ab 0 (Repair re-nummeriert), R07 number als String (validateChapterShape wirft), R08 Case-Duplikat-Titel (lowercase-Dedup greift), R09 einfache Anführungszeichen (repairJson), R10 abgeschnittenes JSON (capTruncatedJson rettet vollständige Kapitel), R11 Trailing Commas, R12 Klartext-Schnorren um JSON (Zustandsmaschine extrahiert), R13 Ein-Wort-Summaries (Gate zählt je Kapitel), R14 Fazit als Kapitel 1, R15 doppelte Fazit-Kapitel, R16 leere Pflichtfelder, R17 chapters als Objekt, R18 Zero-Width-Unicode U+200B in Titeln (BEFUND: Dedup normalisiert KEINE Zero-Width-Zeichen — FMEA-R-14, prio 3), R19 Repair-Hammer Müll→valide (withRetry + STRICT_JSON_SUFFIX bewiesen), R20 50-Kapitel-DoS (Gate stoppt deterministisch, kein Hang/OOM). | Interface-Impact: nein | Tests: 20/20 grün in < 3 s

[AGENT-1] [tests/bookwriter.e2e.simulation.test.ts] NEU (A4): E2E-Simulation mit FakeOllamaProvider + echtem In-Memory-SQLite (sql.js original, Migrationen 18/19/20): E1 Happy Path — 8 Kapitel inkl. Rolling Summary (B1) + Glossar (B2) komplett in 354 ms (< 5 s), Glossar dedupliziert ≤ 30; E2 Kill+Resume — Lauf stirbt nach Kapitel 3, Job-Store hat Fortschritt + Outline committed (setBookJobOutline je Kapitel wie BookWriterPanel), getResumableBookJob liefert status interrupted/current_chapter 3/wiederhergestellte Outline mit 3 Summaries, Resume startet bei 4, Endzustand 8 Kapitel lückenlos [1..8], nach completeBookJob nicht mehr fortsetzbar; E3 Modellwechsel mid-run — config.model llama3.2:latest → mistral:7b ab Kapitel 3, alle Folge-Calls tragen neues Modell (pickModelForTask konservativ Hauptmodell); E4 Abbruch — AbortSignal nach Kapitel 1: generateChapter(Kapitel 2) rejected, keine Zusatz-Calls, bereits generierte Kapitel bleiben als valide Resume-Basis; E5 Chaos random (seed 42) — 8-Kapitel-Lauf überlebt gemischte Fehlerarten (Timeout/Abort/leer/kaputt/huge), status immer draft|needs_revision, kein Wurf, 115 ms. Alle 5 Szenarien ohne echten Ollama → CI-tauglich; Gesamtdauer Suite 3.9 s inkl. Setup. | Interface-Impact: nein | Tests: 5 neue E2E-Szenarien

[AGENT-1] [tests/writing.logic.test.ts] NEU (Coverage-Nachtrag): ideas.ts (generateIdeas je Kategorie count/Titel/Deskription-Vertrag, count > Poolgröße, generateRandomPrompt) und wordstats.ts (Wort-/Satz-/Absatzzählung, Lesezeit 400 Wörter → 2 min mit min 1, Stopwort-Filter in topWords, vocabularyRichness 0-100, längster/kürzester Satz) — beide zuvor 0 % gedeckt. | Interface-Impact: nein | Tests: 9 neue

[AGENT-1] [docs/bookwriter-fmea.md] NEU (A2): FMEA mit 18 Fehlermodi (Spalten Komponente|Fehlermodus|Auswirkung|S/A/D|RPN|Maßnahme, Skala 1-10, RPN=S×A×D). Top-5-Risiken: (1) RPN 84 OllamaProvider.chat Timeout — abgedeckt durch createTimeoutController+withRetry, Restrisiko statische Timeout-Werte; (1) RPN 84 jobs.ts Prozess-Kill — persistNow() pro Kapitel + Resume aus SQLite, bewiesen in E2E-E2; (3) RPN 75 extractEntities Glossar-Müll — Namensdrift still, Maßnahme Glossar-Sanity-Check für Sprint 3 empfohlen; (4) RPN 72 korruptes outline_json bei Resume — rowToJob-Fallback, Maßnahme Outline-Integritätscheck; (4) RPN 72 semantisch kaputte Gliederung — B4-Gate + EIN Repair, Restrisiko Repair-Reproduktion → manueller Eingriff. Weiterhin dokumentiert: Prompt-Injection (48, parametrisierte DB), Retry-Deckel (60), Riesenantwort-Memory (48), ECONNREFUSED-Netzwerkpfad (72), Router-Fallback-Lücken (40). | Interface-Impact: nein

[AGENT-1] [vitest.config.ts] include um "tests/**/*.test.ts" erweitert — Red-Team-/E2E-Suiten liegen bewusst außerhalb src/ (Test-Infrastruktur, kein Produkt-Code, kein Coverage-Beitrag über include src/services/**). | Interface-Impact: nein (nur Test-Glob)

Akzeptanzkriterien Agent 1: FMEA 18 Fehlermodi (≥ 15) + Top-5-Risikoliste ✓ | Red-Team 20/20 grün, jede Injection dokumentiert ✓ | E2E 8 Kapitel < 5 s (354 ms), Kill+Resume, Modellwechsel, Abbruch — läuft ohne echten Ollama in CI ✓ | Coverage src/services/writing/: 88.73 % Lines / 87.11 % Functions / 75.13 % Branches (≥ 85 % Lines ✓) | Gesamtsuite: 1173/1173 grün (103 Dateien), tsc clean, eslint tests clean.

## Sprint 3 — Agent 4: Cover-Prompts, Marketing-Assets & TranslatorService

[AGENT-4] [src/services/bookwriter/coverPrompts.ts + coverPrompts.test.ts] NEU (Teil 1): Visual Prompt Generator — generateCoverPrompts(input, {count, engine}) erzeugt aus der finalen Buch-Zusammenfassung 3-5 hochdetaillierte Prompts für Midjourney ("--ar 2:3 --style raw --v 6") / Stable Diffusion (separater Negative-Prompt). Rein deterministisch (0 LLM-Calls): extractThemes() verdichtet die Summarizer-Zusammenfassung zu inhaltsreichen Schlüsselwörtern (deutsche+englische Stoppwortliste, Häufigkeits-Ranking, case-insensitive Dedup), inferMood() mappt 8 Genre-Familien (Fantasy/Thriller/Krimi/Romance/Horror/SF/Historisch/Sachbuch) auf Stimmung/Palette/Licht, unbekannte Genres über Dunkel-/Hell-Heuristik der Themes; 5 Stil-Spuren (cinematic photographic / symbolisches Objekt / Silhouetten-Figur / illustrated / minimalist) liefern 5 unterscheidbare Kompositionen, jeder Prompt mit SUBJECT/COMPOSITION/LIGHTING/MOOD/PALETTE/MARKET/TITLE AREA-Struktur und Thumbnail-Sicherheit. Fallback bei leerer Zusammenfassung auf Titel/Genre. | Interface-Impact: nein (additiv; konzeptionelles Gegenstück zu llm/covergen.ts für die automatisierte Pipeline) | Tests: 11 neue
[AGENT-4] [src/services/bookwriter/marketingAssets.ts + marketingAssets.test.ts] NEU (Teil 2): Marketing-Assets parallel zum KDP-Preflight — generateMarketingAssets(input) liefert in einem deterministischen Aufruf: (a) Klappentext via bestehendes marketing/blurbgen.generateBlurb (amazon-description-Format, Sätze aus der Zusammenfassung als situation/conflict/stakes), (b) buildKeywords() mit GENAU 7 Amazon-Keywords (KDP-Limit je max. 50 Zeichen, Long-Tail-Kombinationen Genre×Theme, dedupliziert), (c) suggestCategories() mit 3-5 Amazon-KDP-Kategorie-Pfaden ("Fiction > Fantasy > Urban" u.a., 7 Genre-Mappings + generischer Fallback, je mit Begründung). buildMarketingLlmPrompt() liefert den JSON-Prompt für die optionale LLM-Nachschärfung über den Standard-Provider. | Interface-Impact: nein (additiv) | Tests: 9 neue
[AGENT-4] [src/services/bookwriter/markupGuard.ts + translatorService.ts + translatorService.test.ts] NEU (Teil 3): Multilingual-Prep — TranslatorService übersetzt fertige Bücher kapitelweise unter Beibehaltung des Markdown/HTML-Markups. markupGuard: maskMarkup() ersetzt Markup (HTML-Kommentare/Block-Tags inkl. Inhalt, Heading-Präfixe, List-/Quote-Marker, Links, Bilder, Bold/Italic, Inline-Code, Entities) durch ⟦M##⟧-Platzhalter, Heading-/Listen-Text bleibt übersetzbar; restoreMarkup() restauriert (a) überlebende Platzhalter zeichengenau aus der Modell-Antwort, (b) verlorene Block-Präfixe deterministisch aus dem Original (keine heuristische Inline-Rückübersetzung — Inline-Verlust wird stattdessen ehrlich als markupIntact=false gemeldet); markupIntact() verifiziert Struktur-Gleichheit (Headings/Bold/Links/Tags/ListItems). translatorService: translateChapter(chapter, chat, {targetLanguage, glossary?}, signal?) — LLMChatFn-Abstraktion über dem Provider, Prompt mit Markup-Schutzregeln + optionalem Glossar ("Begriff = Übersetzung"), Titel getrennt übersetzt (extractTitleFrom), withRetry (Sprint 1) um den Call; Abort → sauberer AbortError; Netzwerk-/Timeout-/4xx → No-op-Ergebnis (usedProvider: false, Original unverändert), sonst ProviderError. translateBook() übersetzt sequentiell alle Kapitel mit onProgress(completed, total). | Interface-Impact: nein (additiv; kein Bestandsmodul verändert) | Tests: 8 neue (inkl. HTML-Block-Erhaltung, Markup-Verlust-Erkennung, Abort, Buch-Fortschritt)
[AGENT-4] [src/services/bookwriter/index.ts] EXPORTE ergänzt: coverPrompts, marketingAssets, markupGuard, translatorService (public API des Bookwriter-Moduls). | Interface-Impact: ja (neue Exporte, additiv) | Tests: —
[AGENT-4] [LOG] Akzeptanzkriterien Sprint 3 Agent 4: (1) 3-5 detaillierte Cover-Prompts aus der Buch-Zusammenfassung generierbar ✓ (11 Tests, MJ-Parameter + SD-Negative-Prompt geprüft). (2) Klappentext, 7 Keywords, Kategorien automatisch generierbar ✓ (9 Tests, genau 7 Keywords ≤ 50 Zeichen, 3-5 Kategorien mit Pfad). (3) TranslatorService-Struktur mit Kapitel-Markup-Erhaltung ✓ (8 Tests, Headings/Bold/Links/HTML-Blöcke überleben die Übersetzung, Verlust wird erkannt). | Budget: 0 API-Calls (alle drei Module deterministisch bzw. provider-agnostisch über LLMChatFn) | Keine Breaking Changes (nur additive Module + Exporte) | Tests: 1278/1278 grün (114 Dateien, 28 neu), tsc clean (Bestandsfehler in consistency.ts = separater Workstream), eslint clean für alle 7 neuen Dateien

## Sprint 3 — Agent 2: RAG, Long-Term Memory & World Building

[AGENT-2] [src/services/db/migrations/021_memory.ts + index.ts] NEU: Migration 021 — Tabellen bookwriter_facts (id, project_id FK→projects ON DELETE CASCADE, kind, key, value, source_chapter, confidence, created_at/updated_at, UNIQUE(project_id,kind,key)) + bookwriter_consistency_findings (id, run_id, project_id, chapter_index, chapter_title, type, severity, fact_key, expected, found, details, status, created_at). DB-CHANGE: 2 neue Tabellen + Indizes idx_bw_facts_project/idx_bw_facts_kind/idx_bw_consistency_run; schema_migrations Version 21, idempotent. | Interface-Impact: ja (DB-Schema, additiv) | Tests: migration021.test.ts (6 Tests: Tabellen, Spalten, UNIQUE-Upsert-Semantik, Idempotenz, FK-Kaskade)

[AGENT-2] [src/services/bookwriter/contextManager.ts] NEU (Teil 1 — Knowledge-Base Interface): ContextManager speichert projektbezogene Fakten über alle Kapitel hinweg — FactKind character/place/entity/terminology/structure/timeline mit deutschem Label; upsertFact/upsertFacts (Upsert per Projekt+Art+Schlüssel, Validierung: leerer key/value wirft), getFact/listFacts/deleteFact/clearFacts; buildContextBlock() gruppiert deterministisch (Art-Reihenfolge wie FACT_KINDS, Keys alphabetisch, maxPerKind Budget-Schutz Default 10) zu einem verbindlichen Kontextblock "Stabiler Kontext (verbindlich für alle Kapitel)"; extractFactKeys für Eigennamen-Extraktion. Reine DB-/String-Logik, kein LLM-Call, offline deterministisch. | Interface-Impact: ja (neue Service-API, additiv) | Tests: contextManager.test.ts (10 Tests: Upsert-Semantik, Projekt-Trennung, Kontextblock-Gruppierung/Determinismus, Budget-Limit, Stopwort-Filter)

[AGENT-2] [src/services/bookwriter/chapter-gen.ts] Memory-Injektion: generateChapter stellt bei gesetzter projectId den ContextManager-Kontextblock an den Anfang der researchNotes — Fakten bleiben über 50k+ Wörter hinweg im Prompt. Leere Base → Prompt byte-identisch zum Sprint-2-Stand (keine Breaking Changes, kein Prompt-Müll). | Interface-Impact: nein (additiv innerhalb bestehender Signatur) | Tests: chapter-gen.memory.test.ts (3 Tests: Injektion nachgewiesen über abgefangenen Prompt, leere Base unverändert, ohne projectId unverändert)

[AGENT-2] [src/services/llm/router.ts] Sprint 3 (Teil 2 — Spezialisiertes Agenten-Routing): TaskClass logic|creative + TASK_CLASS_MATRIX (entities/repair=logic, chapter/summary/outline/metadata=creative); TaskModelsWithLogic erweitert TaskModels um optionale logic-Rolle (spezialisiertes Logik-/Faktencheck-Modell, z.B. Hermes/Qwen-Instruct); pickModelWithTaskClass wählt Logik-Aufgaben bevorzugt auf dem logic-Modell, Kreativ-Aufgaben unverändert nach Modell-Matrix; ohne logic-Config konservativ bestehendes Verhalten. BookwriterRouter.complete nutzt pickModelWithTaskClass und trägt task_class in RouterCallMeta (Telemetrie). KEINE Breaking Changes: pickModelForTask/MODEL_MATRIX unverändert, meta.task_class optional. | Interface-Impact: ja (additiv: neue Exports + optionales Meta-Feld) | Tests: router.taskclass.test.ts (8 Tests inkl. complete()-Integration: entities→logic-m mit task_class=logic, chapter→main-m mit task_class=creative) + bestehende router.test.ts 12/12 grün

[AGENT-2] [src/services/bookwriter/consistency.ts] NEU (Teil 3 — Konsistenz-Prüfer): checkChapterConsistency prüft fertige Kapitel regelbasiert gegen die Memory-Base: (1) name_drift — Namenskandidaten als Wortfolgen (extractNameSequences, 1–3 Wörter, Stopwort-Filter), normalizeName (Groß/Klein, Umlaute, Zero-Width U+200B–U+200D/BOM, Bindestrich-Varianten U+2010–2015/U+2212), namesLikelySame (exakt, Substring, Levenshtein ≤1, tokenweise) → warning mit expected/found; (2) timeline_break — Jahreszahlen im Text widersprechen timeline-Fakt → error; (3) attribute_conflict — Altersangabe im Text weicht >2 Jahre vom Charakter-Fakt ab → error; Zulässige Kurzformen ("Weber" für "Anna Weber") erzeugen KEINEN Fehlbefund. checkMissingEntities (Buch-Ebene): verbindlicher Charakter (confidence ≥0.9) fehlt in allen Folgekapiteln nach seinem Debüt → warning. runConsistencyCheck persistiert Befunde in bookwriter_consistency_findings und übergibt an den Revisions-Loop (Sprint 2): severity=error → Kapitel-Status needs_revision (updateChapterFields) + findings-Status revision_queued; queueRevision=false nur Prüfung; dismissFinding fürs Review-UI. Ohne Fakten-Base null false positives. Kein LLM-Call — offline deterministisch; optionaler LLM-Faktencheck kann über Router task_class=logic nachgeschaltet werden. | Interface-Impact: ja (neue Service-API, additiv) | Tests: consistency.test.ts (11 Tests: Drift/Zeitlinie/Alter/Kurzform/Missing-Entity, needs_revision-Übergabe, queueRevision=false, leere Base)

[AGENT-2] [src/services/bookwriter/index.ts] Öffentliche API: contextManager + consistency exportiert. | Interface-Impact: ja (additiv) | Tests: —

Akzeptanzkriterien Agent 2: (1) ContextManager speichert/liest projektbezogene Fakten — upsert/get/list/delete/clear + buildContextBlock, 10 Tests ✓ (2) Routing trennt Logik- von Kreativ-Modellen — TASK_CLASS_MATRIX + logic-Rolle in der Router-Kette, entities/repair auf Logik-Modell mit task_class-Telemetrie, 8 Tests ✓ (3) Konsistenz-Prüfer erkennt Namens-/Zeitlinien-Brüche — name_drift/timeline_break/attribute_conflict/missing_entity, Übergabe an Sprint-2-Loop via needs_revision + revision_queued, 11 Tests ✓. Gesamt: 38 neue Tests, Suite 1278/1278 grün, tsc clean (eigene Dateien), eslint clean (eigene Dateien).

## Sprint 6 — Agent 4: Final Build & Dockerization (Finalisierung)

[AGENT-4] [src/services/loggingRuntime.ts + loggingRuntime.test.ts] NEU (TDD RED→GRÜN): App-weite Verdrahtung des Log-Managers — installLogPersistence() erzeugt vor dem ersten Render den LogManager mit Tauri-fs-Adapter (idempotent, no-throw, fire-and-forget in main.tsx), getGlobalLogManager() für globale Fehlerhandler/Diagnose-Panel. Lücke geschlossen: LogManager+logPersistence existierten, waren aber nirgends erzeugt (Totcode — Log-Einträge erreichten die rotierenden Dateien nie). | Interface-Impact: ja (additiv: neue Exports, main.tsx ruft installLogPersistence vor dem ersten Render) | Tests: 4 neue (Erzeugung, Idempotenz, Global-Zugriff, No-Crash)

[AGENT-4] [src/main.tsx] installLogPersistence().catch() vor dem ersten React-Render — Konsolen-Outputs und Fehler landen ab sofort in rotierenden Monatsdateien (app-YYYY-MM.log, 5 MiB-Größenrotation, Retention 10 Dateien/Monat + 180 Tage). | Interface-Impact: nein (Boot-Pfad, fire-and-forget)

[AGENT-4] [package.json] js-yaml@^4.3.2 als devDependency deklariert — der Compose-Contract-Test importierte es bisher nur transitiv (fragil: npm ci-Trim hätte den Test gebrochen). | Interface-Impact: nein (nur devDependencies)

[AGENT-4] [LOG] Akzeptanzkriterien Sprint 6 Agent 4: (1) docker-compose.yml funktioniert OK — Studio (Multi-Stage-Dockerfile, Caddy, 8080:80, ./data/db + ./data/logs-Mounts, Healthcheck) + isoliertes Ollama (eigenes Netz ollama-net + Volume ollama-data, 127.0.0.1:11434, OLLAMA_ORIGINS für Browser-Fetch), 6 Contract-Tests (Docker auf dem Host nicht installiert — strukturelle Verifikation). (2) Log-Rotation OK — logRotation (app-YYYY-MM.log, Kaskade .1/.2, aktive Datei nie geloescht) + logManager (Konsole-Spiegelung + Flush-Schleife) + logPersistence (Tauri-fs) + Verdrahtung über loggingRuntime; bewusst ohne winston/pino (Tauri-WebView, kein Node-FS). (3) npm run release OK — E2E EXIT:0 in 214.9 s: tsc --noEmit, Vite-Build minifiziert (687 Module), Test-Gate 1629/1629 gruen (143 Dateien), Bundle release/ai-writer-studio-1.0.0.zip (77 Dateien, 1.35 MiB, SHA256 81bd194509287a84). | Budget: 0 API-Calls | Keine Breaking Changes | Neue Tests: 32 (22 Logging + 6 Compose-Contract + 4 loggingRuntime), Suite 1629/1629 gruen
